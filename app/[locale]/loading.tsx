import LoaderEffect from "@/components/ui/loader";

const Loading = () => {
	return (
		<div className="h-[80vh] flex justify-center items-center">
			<LoaderEffect />
		</div>
	);
};

export default Loading;
